# 一様分布（Uniform Distribution）
import numpy as np
import matplotlib.pyplot as plt
# import matplotlib

plt.hist(np.random.random(100000), color='aqua')
plt.show()

"""
基本的なカラー名（例：'red', 'green', 'blue', 'cyan', 'magenta', 'yellow', 'black', 'white'）

HTML/CSSカラー名（例：'aqua', 'beige', 'coral', 'fuchsia', 'gold', 'indigo', 'lime', 'navy', 'olive', 'pink', 'silver', 'teal', 'violet'など）

HEXコード（例：'#FF0000' は赤色、'#00FF00' は緑色など）

RGB または RGBA チュープル（例：(1.0, 0.0, 0.0) は赤色、(0.0, 1.0, 0.0, 0.5) は半透明の緑色）
"""